<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Recipes</title>
</head>
<body>
<div class="recipes-container">
    <h1>All Recipes</h1>
    <p>Here are all the recipes you have in your meal planner:</p>
    <ul>
        {{#each recipes}}
            <li>
                <a href="/recipes/{{_id}}">{{title}}</a> - <a href="/recipes/{{this._id}}/edit"><button>Edit</button></a> 

                <form action="/recipes/{{_id}}?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit">Delete</button>
                </form>
            </li>
        {{/each}}
    </ul>
    <a href="/recipes/new"><button>Create a New Recipe</button></a>
    </div>
</body>
</html>

